const antfuOptions = {
  stylistic: true,
  // vue: true, // default not required as it's autodetected by @antfu/eslint-config
  // typescript: true, // default not required as it's autodetected by @antfu/eslint-config
  formatters: {
    css: true,
    html: true,
  },
}

export default antfuOptions
